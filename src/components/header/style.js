import styled from "styled-components";

export const Header = styled.div `
    height: 4rem;
    width: 100vw;
    box-shadow: 0 1px 4px var(--gray);
    display: flex;
    align-items: center;

    h1 {
        padding-left: 1rem;
    }
`